== README
The application generates new ids in every call.
It uses sqllite3 as the storage.
to run it:
<code>rake db:migrate</code>
and then
<code>rails server</code>

in the browser load http://localhost:3000 or make a curl
<code>curl http://localhost:3000 </code>
